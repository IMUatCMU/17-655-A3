package a3.monitor;

/**
 * An interface for a UI component to display itself. This will allow flexible addition of new
 * UI components into the application.
 *
 * @since 1.0.0
 */
public interface MonitorUI {

    void displayUI();
}
